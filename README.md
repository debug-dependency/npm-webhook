# npm-webhook
Installation:
1. Clone the repository or download the files "package.json" and "download.js"
2. Launch CMD and navigate to the directory of npm-webhook.
3. Ensure npm is installed and run this command to install the dependency:
```
npm install .
```
